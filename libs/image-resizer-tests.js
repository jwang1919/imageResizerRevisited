QUnit.module("Image Resizer Tests",function(){test("resizeText()",function(){ok("undefined"!=typeof resizeText,"resizeText() is properly defined. If this fails, fix all JS errors in Cloud9!"),equal(resizeText(100),"Resize to 100px",'resizeText(100) => "Resize to 100px"'),equal(resizeText(200),"Resize to 200px",'resizeText(200) => "Resize to 200px"'),equal(resizeText(300),"Resize to 300px",'resizeText(300) => "Resize to 300px"'),equal(resizeText(400),"Resize to 400px",'resizeText(400) => "Resize to 400px"'),expect(5)}),test("createButton()",function(){ok("undefined"!=typeof createButton,"createButton() is properly defined. If this fails, fix all JS errors in Cloud9!"),equal(createButton(100),"<button id='button100'>Resize to 100px</button>","createButton(100) => \"<button id='button100'>Resize to 100px</button>\""),equal(createButton(200),"<button id='button200'>Resize to 200px</button>","createButton(200) => \"<button id='button200'>Resize to 200px</button>\""),equal(createButton(300),"<button id='button300'>Resize to 300px</button>","createButton(300) => \"<button id='button300'>Resize to 300px</button>\""),equal(createButton(400),"<button id='button400'>Resize to 400px</button>","createButton(400) => \"<button id='button400'>Resize to 400px</button>\"");var e=Dexter.fake(window,"resizeText");createButton(),equal(e.called,1,"resizeText() should be called in createButton()"),e.restore(),expect(6)}),test("calculateImageSize()",function(){ok("undefined"!=typeof calculateImageSize,"calculateImageSize() is properly defined. If this fails, fix all JS errors in Cloud9!"),equal(calculateImageSize(1),100,"calculateImageSize(100) => 100"),equal(calculateImageSize(2),200,"calculateImageSize(200) => 200"),equal(calculateImageSize(3),300,"calculateImageSize(300) => 300"),equal(calculateImageSize(4),400,"calculateImageSize(400) => 400"),expect(5)}),test("addButton()",function(){ok("undefined"!=typeof addButton,"addButton() is properly defined. If this fails, fix all JS errors in Cloud9!");var e=Dexter.fake(window,"createButton");addButton(),equal(e.called,1,"createButton() should be called in addButton()"),e.restore(),addButton(100),addButton(200),addButton(300),addButton(400),ok($("#button100").length>0,"button with id=button100 found"),ok($("#button200").length>0,"button with id=button200 found"),ok($("#button300").length>0,"button with id=button300 found"),ok($("#button400").length>0,"button with id=button400 found"),$("#button100").click(),equal($("img").width(),100,"Width expected to change to 100px when button100 is clicked"),$("#button200").click(),equal($("img").width(),200,"Width expected to change to 200px when button200 is clicked"),$("#button300").click(),equal($("img").width(),300,"Width expected to change to 300px when button300 is clicked"),$("#button400").click(),equal($("img").width(),400,"Width expected to change to 400px when button400 is clicked"),expect(10)}),test("runForLoop()",function(){ok("undefined"!=typeof runForLoop,"runForLoop() is properly defined. If this fails, fix all JS errors in Cloud9!");var e=Dexter.fake(window,"calculateImageSize"),t=Dexter.fake(window,"addButton");runForLoop(),equal(e.called,4,"calculateImageSize() should be called in runForLoop()"),equal(t.called,4,"addButton() should be called in runForLoop()"),e.restore(),t.restore(),equal(calculateImageSize(1),100,"calculateImageSize(100) => 100"),equal(calculateImageSize(2),200,"calculateImageSize(200) => 200"),equal(calculateImageSize(3),300,"calculateImageSize(300) => 300"),equal(calculateImageSize(4),400,"calculateImageSize(400) => 400"),expect(7)})});
